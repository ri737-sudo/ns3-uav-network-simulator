It models UAV-to-UAV communication, packet drops due to obstacles (urban vs open terrain), and height-based fading. FlowMonitor is integrated to measure throughput, delay, transmitted/received packets, and packet loss. The simulator is fully configurable via command-line parameters such as number of UAVs, terrain type, and message payload.It models UAV-to-UAV communication, packet drops due to obstacles (urban vs open terrain), and height-based fading. FlowMonitor is integrated to measure throughput, delay, transmitted/received packets, and packet loss. The simulator is fully configurable via command-line parameters such as number of UAVs, terrain type, and message payload.
